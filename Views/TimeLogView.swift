import SwiftUI

struct TimeLogView: View {
    var body: some View {
        NavigationView {
            Text("Welcome to TRACKTOR!")
                .navigationTitle("Time Log")
        }
    }
}

#Preview {
    TimeLogView()
}